# Predictive pacing notes

Implemented predictive pacing behavior now lives in this directory:

- `predictive-pacing.md` — current engine inputs, decisions, and implementation
  status
- `floating-overlay.md` — overlay-specific alert styling and ambient zone-bar
  behavior

Keep this file only for future design notes that are not yet part of the
implemented spec. If it starts describing current behavior again, move that
content into the relevant implemented spec file in this directory instead.
